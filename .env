VITE_USE_MOCK=false
VITE_API_PROXY_TARGET=http://127.0.0.1:3000
# Gaode (AMap) Web JS API — property map picker; GCJ-02 matches WeChat mini program + Gaode SDK.
# Create keys at https://console.amap.com/ → Application management → Key (Web端 JS API).
VITE_AMAP_WEB_KEY=
# If your Gaode console enables "security key" for JS API, paste it here (set before map script loads).
VITE_AMAP_SECURITY_JS_CODE=
